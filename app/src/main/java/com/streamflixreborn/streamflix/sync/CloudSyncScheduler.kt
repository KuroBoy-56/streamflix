package com.streamflixreborn.streamflix.sync

import android.content.Context

object CloudSyncScheduler {
    fun enqueue(context: Context) {
        // No requerido con Firebase Realtime Database
    }
}
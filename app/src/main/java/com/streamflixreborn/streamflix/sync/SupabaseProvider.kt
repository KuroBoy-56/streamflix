package com.streamflixreborn.streamflix.sync

import android.content.Context

object SupabaseProvider {
    val isConfigured: Boolean = false
    fun initialize(context: Context) {
        // Reemplazado por Firebase
    }
}